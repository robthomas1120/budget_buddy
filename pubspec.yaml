name: Budget_Buddy
description: A finance tracking app for iOS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # UI
  cupertino_icons: ^1.0.5
  flutter_slidable: ^2.0.0
  fl_chart: ^0.55.0
  
  # Database
  sqflite: ^2.2.8+4
  path: ^1.8.3
  
  # Utilities
  intl: ^0.18.1
  shared_preferences: ^2.1.1
  provider: ^6.0.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
